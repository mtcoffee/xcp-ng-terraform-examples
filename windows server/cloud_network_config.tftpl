version: 2
ethernets:
    eth0:
        dhcp4: false
        dhcp6: false
        addresses:
          - '${ip}'
        gateway4: '${gw}'
        nameservers:
          addresses:
            - '${dns}'
          search:
            - '${searchdomain}'